# 📝 AI 서술형 평가 시스템 (AI Essay Assessment)

이 프로젝트는 **Streamlit**을 기반으로 구축된 **학생용 서술형 평가 대시보드**입니다.
학생이 답안을 제출하면 **OpenAI API(GPT)**가 실시간으로 채점하고, 결과는 **Supabase** 데이터베이스에 자동으로 저장됩니다.

## 📊 시스템 구조 (Architecture)

아래 다이어그램은 데이터의 흐름과 시스템 구성을 나타냅니다.

```mermaid
graph TD
    User(["👤 학생"]) -->|"1. 답안 작성 & 제출"| App["💻 Streamlit App"]
    
    subgraph "Backend Process"
        App -->|"2. 프롬프트 전송"| AI["🧠 OpenAI API (GPT-4o)"]
        AI -->|"3. 채점 결과 반환"| App
        App -->|"4. 데이터 저장"| DB[("🗄️ Supabase")]
    end
    
    App -->|"5. 실시간 피드백 표시"| User
